/**
 * Shared chrome for main-tab (and sibling) screens so top headers align with Home.
 * Safe-area top is applied separately as paddingTop: insets.top on the header container.
 */
/** Taller strip so centered titles clear Dynamic Island; bottom padding aligns tab chrome. */
export const MAIN_TAB_TOP_BAR_MIN_HEIGHT = 60;
export const MAIN_TAB_TOP_BAR_PADDING_BOTTOM = 12;
export const MAIN_TAB_TOP_BAR_PADDING_H = 16;

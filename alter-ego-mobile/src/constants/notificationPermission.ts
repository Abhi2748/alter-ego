/**
 * Key used in AsyncStorage to remember that we already showed the notification
 * permission screen (after Twin Introduction). Once set, we skip that screen.
 */
export const NOTIF_PERMISSION_ASKED_KEY = "notif_permission_asked";
